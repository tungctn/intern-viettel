version: 0.2
phases: 

  install:
    runtime-versions:
      nodejs: 16
    commands:
      - cd my-app
      - npm install
  
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: 'my-app/build'

cache:
  paths:
    - 'my-app/node_modules/**/*'

